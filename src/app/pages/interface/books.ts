export interface IBookList {
   name?: string,
   img: string
}

export interface IBook {
  name: string,
  title: string,
  description: string,
  price: string,
  img: string,
  language: string,
  id: string
}